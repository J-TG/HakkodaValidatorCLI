definition_version: '2'
entities:
  streamlit_cli_deploy:
    type: streamlit
    identifier:
      name: streamlit_cli_deploy
    main_file: streamlit_app.py
    pages_dir: pages
    query_warehouse: COMPUTE_WH
    stage: STREAMLIT_CLI
    artifacts:
      - streamlit_app.py
      - environment.yml
      - pages
      - common

